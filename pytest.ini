[pytest]
# Tests import the project's packages (speech, routes, database, storage) by
# name. Without this, a bare `pytest` fails collection with ModuleNotFoundError
# and everyone has to remember PYTHONPATH=. instead.
pythonpath = .
testpaths = tests
